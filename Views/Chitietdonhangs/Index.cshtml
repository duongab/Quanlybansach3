@model IEnumerable<Quanlybansach.Models.Chitietdonhang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Donhang.tinhtrangdonhang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sach.tensach)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dongia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Donhang.tinhtrangdonhang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sach.tensach)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dongia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.soluong }) |
            @Html.ActionLink("Details", "Details", new { id=item.soluong }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.soluong })
        </td>
    </tr>
}

</table>
